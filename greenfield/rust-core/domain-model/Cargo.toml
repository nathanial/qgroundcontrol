[package]
name = "qgc-domain"
version = "0.1.0"
edition = "2021"
authors = ["QGroundControl Team"]
description = "Shared domain models for the QGC greenfield core"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
